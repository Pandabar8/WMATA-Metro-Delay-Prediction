venv/
docs/superpowers/
.claude/
.idea/
__pycache__/
*.pyc
.env
.DS_Store

# Data — exclude large DB, zips, and exports
data/wmata.db
data/*.zip
data/*.csv
data/*.log
# ...except incidents.csv, which is small (~6 MB) and lets collaborators
# run the event-study notebook without needing the full DB.
!data/incidents.csv
